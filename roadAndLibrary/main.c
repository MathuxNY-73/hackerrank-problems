#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
    int q; 
    scanf("%i", &q);
    for(int a0 = 0; a0 < q; ++a0){
        int n; 
        int m; 
        long int x; 
        long int y; 
        scanf("%i %i %li %li", &n, &m, &x, &y);
        for(int a1 = 0; a1 < m; a1++){
            int city_1; 
            int city_2; 
            scanf("%i %i", &city_1, &city_2);
        }
    }
    return 0;
}